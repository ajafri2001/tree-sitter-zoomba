retry_config = { "max" : 1 , "interval" : 10 }
#(x ? e ) = #retry ( retry_config ) { 1/0  }
assert( size(e.cause.failures) == 2, "Tries does not match!")

#(x ? e ) = #retry ( max = 2, interval = 10 ) { 1/0  }
assert( size(e.cause.failures) == 3, "Tries does not match!")

#(x ? e ) = #retry ( { "max": 1, "interval": 20 } ) { 42  }
assert( x == 42, "retry is not returning result")
